// {{ ansible_managed }}
// Embedded exporter only (ROADMAP.md Backlog №34) — компоненты не сконфигурированы, alloy
// экспортирует только собственные метрики (alloy_build_info и т.п.) на своём HTTP-сервере.
