---
- name: Converge
  hosts: samba_servers
  become: true
  collections:
    - devops.uslugi
  tasks:
    - name: Прогнать роль samba_server (переменные — см. group_vars/all.yml)
      ansible.builtin.import_role:
        name: samba_server
