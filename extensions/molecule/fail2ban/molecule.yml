---
# Molecule-сценарий для fail2ban — driver: vagrant/libvirt (единое требование для всех
# molecule-сценариев коллекции, см. CLAUDE.md, «Molecule-тесты»).
driver:
  name: vagrant
  provider:
    name: libvirt

platforms:
  - name: fail2ban
    box: cloud-image/ubuntu-24.04
    memory: 1024
    cpus: 1

scenario:
  test_sequence:
    - dependency
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - destroy

shared_state: false

ansible:
  executor:
    args:
      ansible_playbook: []
