---
- name: Converge
  hosts: all
  become: true
  collections:
    - devops.uslugi
  tasks:
    - name: Прогнать роль fail2ban (переменные — см. group_vars/all.yml)
      ansible.builtin.import_role:
        name: fail2ban
